# Single source of truth for language/tool versions (asdf/mise format).
# Humans and AI agents should use these versions for all builds, tests, and tooling.
# Install with `mise install` (or `asdf install`).
cmake 4.4.3
ninja 1.13.2
